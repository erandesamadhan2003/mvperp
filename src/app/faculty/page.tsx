export default function FacultyPage() {
    return (
        <section className="space-y-3">
            <h2 className="text-2xl font-bold text-slate-900">Welcome, Faculty</h2>
            <p className="text-sm text-slate-600">
                Open your module from the sidebar. Your menu is loaded dynamically from the login session.
            </p>
        </section>
    );
}
